<%= simple_form_for user, url: reset_password_lender_user_path(user), method: :post do |f| %>
  <%= f.button :submit, class: 'btn btn-info', value: 'Send Reset Password Email', confirm: "Are you sure you want to send a password reset email to #{user.name}?" %>
<% end %>

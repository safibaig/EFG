<%= simple_form_for(@eligibility_decision_email, url: email_loan_eligibility_decision_path, html: { class: 'form-horizontal' }) do |f| %>

  <%= f.input :email %>

  <div class="form-actions">
    <%= f.button :submit, value: 'Send', class: 'btn-primary', data: { 'disable-with' => 'Submitting...' } %>
  </div>

<% end %>

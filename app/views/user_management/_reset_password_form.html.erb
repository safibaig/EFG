<%= simple_form_for user, url: url_for(action: :reset_password, id: user), method: :post do |f| %>
  <%= f.button :submit, class: 'btn btn-info', value: 'Send Reset Password Email', data: { 'confirm' => "Are you sure you want to send a password reset email to #{user.name}?" } %>
<% end %>
